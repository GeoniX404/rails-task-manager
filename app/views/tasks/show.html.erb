<div class="container">
  <h1><%= @task.title %></h1>
  <%= link_to "Edit this task", edit_task_path %>
  <p><%= @task.details %></p>
  <p class="status">Status: <%= @task.completed ? "Completed." : "Not completed." %></p>
  <%= link_to "Back to tasks", tasks_path %>
</div>
